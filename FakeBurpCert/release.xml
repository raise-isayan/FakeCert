<?xml version="1.0" encoding="UTF-8"?>
<project name="FakeBurpCert" default="release" basedir="." >

    <!-- Load our properties -->
    <property file="src/release.properties"/>
    <property name="dist.dir" value="."/>
    <property name="dist.zip" value="${ant.project.name}.${version}.zip"/>

    <target name="release">
        <antcall target="margepkg"/>
        <antcall target="releasepkg"/>
    </target>

    <target name="margepkg">
        <jar destfile="release/${ant.project.name}.jar" update="no"  manifest="META-INF/MANIFEST.MF">
            <zipgroupfileset dir="dist/" includes="*.jar" />
        </jar>
    </target>

    <target name="releasepkg">
        <zip basedir="." destfile="${dist.zip}" update="false">
            <include name="**"/>
            <exclude name="dist/**"/>
            <exclude name="nbproject/configs/**"/>
            <exclude name="nbproject/private/**"/>
            <exclude name="build/**"/>
            <exclude name="**/.svn"/>
            <exclude name="**/.git"/>
            <exclude name="*.zip"/>
            <exclude name="*.log"/>
            <exclude name="*.log.lck"/>
        </zip>
    </target>
    
    <target name="clean">
      <delete dir="/build"/>
      <delete dir="/dist"/>
    </target>
    
</project>
